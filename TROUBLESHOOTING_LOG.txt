================================================================================
VOICE CHAT AI - AUDIO OUTPUT TROUBLESHOOTING LOG
Date: May 10, 2026
================================================================================

PROBLEM STATEMENT:
The application successfully receives voice input, transcribes it, and receives
LLM responses. The UI displays text output correctly. However, the text-to-speech
(TTS) audio output is NOT working - no audio is heard from the AI response.

================================================================================
KEY OBSERVATION:
The test audio button WORKS - it successfully plays a 440Hz tone through the
speakers. This proves:
  ✓ Audio output device is reachable
  ✓ Speakers are functioning
  ✓ The audio subsystem is working

CONCLUSION: The problem is NOT with audio routing/device access. The problem
is specifically with how the TTS is being generated or played within the app.

================================================================================
ATTEMPTS MADE:
================================================================================

ATTEMPT 1: Initial Pygame Rebuild (Fixed Hardcoded Devices)
------------------------------------------------------------------------
Issue: Original tkinter app had hardcoded device IDs (device=15 and device=16)
  
Action Taken:
  - Created pygame version of the app
  - Ran diagnostic: test_audio_devices.py
  - Found default output device is device 21
  - Removed hardcoded device=15/16
  - Updated _speak() and _play_beep() to use default device
  
Result: FAILED - Audio still not playing

Status: ✗ Did not fix the issue


ATTEMPT 2: Word-by-Word Synthesis with Delays
------------------------------------------------------------------------
Hypothesis: Maybe synthesizing entire text at once was causing issues.
  
Action Taken:
  - Modified _speak() to synthesize individual words
  - Added 0.2s pause between words
  - Added word highlighting feature to UI
  - Used blocking sd.wait() for each word
  
Result: FAILED - No audio output, process crash with audio mutex error
  Error: "pa_unix_util.c:510: PaUnixMutex_Terminate: Assertion `0 == paUtilErr_' failed"
  
Status: ✗ Worse than before - caused threading/mutex issues


ATTEMPT 3: Full Text Synthesis with Async Highlighting
------------------------------------------------------------------------
Hypothesis: Threading issue with sounddevice. Try separate thread for playback.
  
Action Taken:
  - Synthesize full text once
  - Play audio in background thread
  - Highlight words on main timer while audio plays
  - Removed stream manipulation (stop/start)
  
Result: FAILED - Audio still not audible
  
Status: ✗ Did not fix the issue


ATTEMPT 4: System Audio Player (paplay)
------------------------------------------------------------------------
Hypothesis: sounddevice.play() has conflicts with recording InputStream.
  Use system command instead.
  
Action Taken:
  - Replaced sounddevice.play() with subprocess.Popen(["paplay", wav_path])
  - Kept word highlighting synchronized with process timing
  - Avoided any direct sounddevice interactions during playback
  
Result: PENDING - Need user testing
  
Current Status: ⚠ Unknown if this works - awaiting feedback


================================================================================
TECHNICAL DETAILS:
================================================================================

File Structure:
  - main.py (entry point - runs voice_chat_client_pygame.py)
  - voice_chat_client_pygame.py (main application)
  - test_audio_devices.py (diagnostic tool)

Key Methods Involved:
  - _speak(text, lang, line_idx) - Generates and plays TTS
  - _load_models() - Loads Piper voice during startup
  - _play_beep() - Test audio button (WORKS)

TTS Engine: Piper (offline, runs locally)
Voice Files:
  - fr_FR-siwis-medium.onnx (French female voice)
  - en_US-ljspeech-high.onnx (English female voice)

================================================================================
IMPORTANT NOTE:
================================================================================

The fact that the TEST AUDIO BUTTON works proves:
  1. Audio device selection is correct
  2. Audio routing is working
  3. Speakers are functional
  4. System audio subsystem is operational

Since test audio works but TTS does not, the issue must be in one of these areas:
  - Piper voice synthesis (generating incorrect or empty audio)
  - Temporary file handling (audio file not being created properly)
  - Audio duration calculation (timing issues for highlighting)
  - The interaction between TTS generation and playback

DO NOT assume it's an audio device/routing issue. It's not. Focus on the TTS
pipeline specifically.

================================================================================
NEXT STEPS TO TRY:
================================================================================

1. Debug the actual audio file generation:
   - Save synthesized WAV to a known location
   - Verify it's not empty or corrupt
   - Manually play it with paplay to test

2. Add logging to _speak():
   - Log when Piper starts synthesis
   - Log file size after synthesis
   - Log when subprocess.Popen starts
   - Log process return codes

3. Check if Piper is actually being called:
   - Verify voice.synthesize() completes without errors
   - Check if exception is being silently caught

4. Test voice.synthesize() directly:
   - Create a minimal script that ONLY does TTS
   - Don't involve pygame, threading, or complex logic
   - Verify Piper can generate audio at all

5. Alternative TTS engine:
   - Consider switching from Piper to espeak-ng or similar
   - If Piper is fundamentally broken in this environment, try different TTS

================================================================================
CURRENT APP STATUS:
================================================================================
✓ Speech recognition (Whisper) - WORKING
✓ Language detection - WORKING
✓ LLM integration (thebrain) - WORKING
✓ Text display - WORKING
✓ UI/Pygame rendering - WORKING
✓ Test audio button - WORKING
✓ Audio device routing - WORKING

✗ TTS synthesis/playback - NOT WORKING (requires debugging)

================================================================================
